# 这次修改做了什么

- 把 Python 网页原型移植为独立 SwiftUI 菜单栏 App。
- 增加原生数据采集、缓存、三类概率、15 分钟刷新和系统通知。
- 增加 App 图标、Release 打包、临时签名和安装维护文档。

## 相关 Issue

Issue 2：制作原生 macOS 菜单栏 App。

## 为什么这样做

用户需要的是无需浏览器和终端、点击菜单栏即可查看的真正 `.app`。SwiftUI
可以直接提供菜单栏窗口、系统通知和 macOS 应用生命周期。

## 怎样检查

- [x] `git diff --check` 通过
- [x] Swift 6 严格编译通过
- [x] Swift 自动测试通过
- [x] Release 构建、Info.plist 检查和临时签名通过
- [x] 真实公开数据可生成概率快照
- [x] 2x 视觉快照已检查，信息完整且无明显裁切
- [ ] 用户点击实际菜单栏图标并确认交互

手动检查步骤：

1. 启动 `Tibo Radar.app`，确认只有菜单栏雷达图标，没有 Dock 图标。
2. 点击图标，检查四类概率、证据、刷新按钮和退出按钮。
3. 断网后刷新，确认显示缓存提示且 App 不退出。

## 可能影响什么

- [x] 只增加公开缓存，卸载和清理位置已写明
- [x] 不改变外部接口
- [x] 不增加环境密钥或账号配置
- [x] 只新增系统通知权限，已写明用途
- [x] 本机临时签名的分发限制已写明

## 合并前自查

- [x] 已经从头到尾查看所有文件变化
- [x] 没有无关文件、调试缓存和临时日志
- [x] 没有密码、密钥、个人数据和敏感日志
- [x] README、架构、需求、Issue 和版本记录已同步
- [ ] Issue 中需要用户验收的完成条件已满足

## 合并确认

- [ ] 用户已经明确表示“确认”“接受”或“合并”这个实际 App
- [x] 当前改动保留在 `feature/2-native-menubar`，未合并到 `main`
