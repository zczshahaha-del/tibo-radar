# 这次修改做了什么

- 用 DeepSeek / 千问 AI 调用替换原生 App 的关键词规则预测器。
- 增加供应商切换、模型名称、用户自带 Key、macOS 钥匙串和连接测试界面。
- 千问开启百炼联网搜索；DeepSeek 分析 App 实时采集的公开原文。
- 增加统一 JSON 预测结构、范围校验、AI 快照回退和后台费用节流。

## 相关 Issue

Issue 3：用 DeepSeek / 千问替换规则预测核心。

## 为什么这样做

用户明确否决关键词加减分，要求 AI 根据 Tibo 社交动态、官方信号和网络讨论
作出语义判断，并确认自己填写 DeepSeek 或千问 API Key。

## 怎样检查

- [x] `git diff --check` 通过
- [x] Swift 6 严格编译通过
- [x] 18 个 Swift 测试通过；普通测试中 3 个按需检查被跳过
- [x] 真实公开数据源测试单独通过
- [x] AI 主面板和设置页 2x 视觉快照已检查
- [x] Release arm64 App 构建和临时签名检查通过
- [ ] 用户分别用自己的 DeepSeek / 千问 Key 完成“保存并测试”
- [ ] 用户生成至少一次真实 AI 预测并检查证据

## 可能影响什么

- [x] API Key 只存 macOS 钥匙串，不进入 UserDefaults、缓存或日志
- [x] AI 快照按供应商分开保存，可以回退但会明确标记过期
- [x] 千问使用百炼中国站接口；其他地域 Key 可能鉴权失败
- [x] 每次手动刷新会产生模型费用；后台对相同资料限制调用频率
- [x] 不登录 X，不自动兑换或操作用户账户
- [x] 当前仍为 Apple Silicon、本机临时签名验收包

## 合并前自查

- [x] 已查看本分支全部文件变化
- [x] 没有真实密钥、个人数据、调试缓存或临时视觉文件
- [x] README、需求、架构、使用说明、决策和版本记录已同步
- [x] 正式 App 已删除 Swift 规则预测器，不会静默回退规则概率
- [ ] Issue 3 的真实模型验收条件已满足

## 合并确认

- [ ] 用户已经明确表示“确认”“接受”或“合并”这个 AI 版本
- [x] 当前修改只保留在 `feature/3-ai-provider-prediction`，未合并到 `main`
