# 用普通话解释预测时间和可靠性

## 问题现象

AI 预测面板右侧显示“高发时段”和“置信度”，实际结果出现
“若发生，最可能落在此……”的截断文字以及单独一个“低”。用户无法判断
它们分别在说什么，也看不到为什么可靠性低。

## 环境

- Tibo Radar v0.3.0 候选版
- macOS 菜单栏弹出面板
- DeepSeek AI 快照

## 真正原因

- “高发时段”“置信度”是分析术语，没有解释成用户要做的判断。
- 时间字段只允许显示一行，AI 返回长句时被省略号截断。
- `confidenceNote` 已保存在快照中，但界面没有展示，所以“低”没有原因。
- 提示词没有强制时间字段使用短格式，模型可能返回一整句话。

## 这次要做什么

- 把“高发时段”改成“最可能什么时候”。
- 没有可靠时间时统一显示“暂时算不出”，有时间时允许两行完整显示。
- 把“置信度”改成“这个判断靠谱吗”，显示“比较靠谱 / 仅供参考 / 证据很弱”。
- 在主概率下面直接显示一条简短说明，解释可靠性为什么高或低。
- 收紧 AI 提示词，要求时间只返回简短北京时间或“暂无可靠时间”。
- 增加文字转换和视觉快照测试。

## 怎样判断完成

- [x] 面板不再出现“高发时段”“置信度”等未解释术语。
- [x] 时间内容不会因为单行限制而只剩省略号。
- [x] 可靠性高低有可见原因，不需要用户猜。
- [x] 旧缓存中的长句也能显示成可理解的短文字。
- [x] 自动测试、视觉检查和 Release 构建通过。
- [ ] 用户检查新版界面后决定是否接受。

## 风险

- 只改显示和 AI 输出要求，不改变概率计算、API Key 或缓存格式。
- 旧快照继续兼容，不需要清理用户缓存。

## 开发记录

- 2026-09-11：截图确认问题来自术语、单行截断和没有展示
  `confidenceNote`，不是用户理解问题。
- 2026-09-11：增加统一文字转换；无可靠时间显示“暂时算不出”，可靠性显示
  “比较靠谱 / 仅供参考 / 证据很弱”，并把具体原因放到主要依据前面。
- 2026-09-11：首次全量测试出现 `XCTAssertFalse failed`。原因是旧测试把来源
  时间固定为 2026-09-10，却使用真实当前时间判断数据新鲜度，过零点后测试
  自然变成过期。已把测试时钟固定为同一天，避免以后随日期随机失败。
- 2026-09-11：21 个 Swift 测试通过，2x 视觉快照确认新文字没有截断；
  v0.3.1 Release App 构建、启动、解压复验和临时签名检查通过。候选包
  SHA-256 为
  `3269c379f63542a7108ca7bc5d56462e6ca575ffebe4d625cb9840bb7d3a35ec`。
- 2026-09-11：用户验收时指出“30% + 证据很弱”仍然没有决策价值。
  进一步检查确认，30% 来自模型自报而非历史回测校准；单纯补充可靠性文案
  无法解决假精确问题。v0.3.1 未被接受，不得合并；核心修正转入 Issue 5。
