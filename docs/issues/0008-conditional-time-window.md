# 概率与发生时段必须表达同一件事

## 出现了什么

界面一边显示未来 24 小时只有较低概率，另一边写“最可能什么时候：9月11日
07:00–10:00”。AI 的本意是“假如事件会发生，这个时段相对更集中”，但界面
把条件省掉后，看起来像在断言今天早上大概率会发生。

## 影响

- 影响谁或什么功能：所有根据预测安排 Codex 用量的用户。
- 严重程度：严重，会直接改变用户对时间的理解。
- 出现频率：AI 返回具体时间窗口时每次发生。
- 是否影响数据或安全：不影响数据安全，但会误导决策。

## 真正原因

事件发生概率与“已经假设事件发生后，它更偏向哪个时段”是两个不同问题。
数据结构和界面都叫 `likely_window`，没有保留这个条件，导致文字比概率更肯定。

## 解决方法

- 主预测显示 AI 的中心估计，同时把上下范围作为“可能范围”放在次要位置。
- 时间字段改为条件时段：只回答“如果 24 小时内发生，更偏向哪个时间”。
- 界面明确显示“若发生，较可能在”，不再写“最可能什么时候”。
- 没有集中时段时显示“暂无集中时段”，不能编造具体时间。
- AI 不得返回早于本次预测生成时间的窗口。

## 怎样确认已经解决

- [x] 主区域同时看得到中心估计与概率范围，主次清楚。
- [x] 全局、普发卡的中心估计与范围都能核对。
- [x] 时间标签明确带有“若发生”的条件。
- [x] 低概率与具体条件时段同屏时不会像确定日程。
- [x] 自动测试、真实 DeepSeek、视觉快照和 Release 构建通过。
- [ ] 用户检查新版后明确接受，才允许合并。

## 相关记录

- 2026-09-11：29 个 Swift 自动测试通过；真实公开源和已保存 DeepSeek Key 的
  新 `conditional_window` 结构调用通过。
- 2026-09-11：v0.5.1 的 2x 视觉快照、Release 构建、启动、压缩包完整性、
  版本、arm64 架构和临时签名复验通过。候选包 SHA-256 为
  `6f86c5120a4a82296254dee276e2aa08459e9d4b24f2de23f7651fa7ab1d7e4a`。
- PR：`docs/pull-requests/0008-conditional-time-window.md`。
- Commit：`f1daa12`。
- 参考：Issue 7，决策 6。
