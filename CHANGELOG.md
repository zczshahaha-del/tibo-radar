# 版本变化记录

## 尚未发布

### v0.5.1 候选版

- 主数字改为 AI 中心估计，下面同时保留“可能范围”，避免只能看到一个宽区间。
- 把“最可能什么时候”改为“若发生，较可能在”，明确它是已经假设事件发生后
  的条件时段，不是对某个时间点的确定预告。
- 没有明显集中的时间窗口时显示“暂无集中时段”。
- AI 输出字段改为条件时段语义，并要求不得返回早于本次预测时间的窗口。

### v0.5.0 候选版

- DeepSeek / 千问每次刷新都必须给出全局重置、普发重置卡和综合事件的
  24/48 小时概率范围；没有新信号也不能取消预测或返回全零。
- 把 307 个历史回放时段形成的动态范围交给 AI 作为基准，再由 AI 综合近期
  言论、服务状态、缺少新动态和反面证据形成最终预测。
- 每个预测内部包含低值、最可能值和高值；界面只显示范围，避免假精确单点。
- 增加范围契约校验：48 小时不能低于 24 小时，综合事件不能低于任一分类。
- 主界面改成“AI 综合预测”，通知改为在 AI 综合概率跨过阈值时触发。

### v0.4.1 候选版

- 恢复有历史回放支撑的动态 24/48 小时概率范围，并明确标成“历史估计”。
- 历史概率与 AI 新信号分层展示；AI 没有新信号时仍然有可变化的预测范围。
- 只有回放包含 AI 信号且击败旧模型时才标成“已校准”，不做假数值修正。
- 缺失、过少、无效、过期或不如简单猜测的历史数据仍然不会显示数字。

### v0.4.0 候选版

- 删除 DeepSeek / 千问自报的 0–100 概率，AI 只判断无、弱、强或明确预告。
- 增加独立概率质量门：必须包含 AI 语义信号、至少 100 个历史时间窗并击败
  两个基线；未通过时明确显示“暂无可靠概率”。
- 概率通过质量门后只显示范围和样本量依据，不显示假精确的单点数字。
- 通知改为响应新强信号、明确预告或确认事件，不再依赖未校准百分比阈值。
- 旧版 AI 概率缓存不再读取，避免升级后继续显示旧数字。

### 新增

- 增加 DeepSeek 与千问 AI 预测核心，用户可以自行填写并切换 API Key。
- 增加 macOS 钥匙串密钥保存、连接测试和按供应商分离的 AI 快照缓存。
- 千问调用开启百炼联网搜索；DeepSeek 分析 App 实时采集的公开原文。
- 增加固定 JSON 结果校验、鉴权和限流错误提示，AI 失败时只显示过期快照。
- 后台每 15 分钟巡检资料；资料不变时最多每小时调用一次 AI，手动刷新不受限。
- 增加独立的 SwiftUI 菜单栏 App，无需浏览器、终端或 Python。
- 增加原生菜单栏面板、15 分钟刷新、手动刷新和 macOS 系统通知。
- 增加 App 图标生成、Release `.app` 组装和本机临时签名脚本。
- 增加 Swift 预测、离线缓存回退、通知去重、真实接口和视觉快照测试。
- 增加公开 forecast、Tibo 时间线和 OpenAI Status 的并行采集与本地缓存。
- 增加可解释的 24 / 48 小时全局重置、普发重置卡和综合概率。
- 增加定向故障补发识别，避免把条件福利误算成普发福利。
- 增加本地网页、命令行快照、macOS 阈值通知和双击启动脚本。
- 增加预测、缓存回退与通知去重测试。

### 修改

- 原生 App 正式概率改由 AI 生成，删除 Swift 关键词规则预测器及其规则测试。
- 版本升级到 v0.3.0。
- 原生 App 成为主要交付，Python 网页版本保留为规则对照原型。
- 兼容 macOS 系统自带 Python 3.9，无需第三方运行依赖。

### 修复

- 把难懂且会截断的“高发时段 / 置信度”改为“最可能什么时候 / 这个判断
  靠谱吗”，并直接显示可靠性原因；旧 AI 快照同样适用。
- 修复 AI 快照测试使用真实当前时间，导致跨日期后把固定测试数据误判为过期
  的问题。
- 修复菜单栏视觉快照中滚动区域不渲染的问题，改用固定紧凑布局。
- 修复 Swift 临时编译文件误入 Git 和图标临时目录导致打包失败的问题。
- 修复非 `watch` 命令被刷新间隔校验误拦截的问题。

### 安全更新

- API Key 不进入 UserDefaults、缓存、日志或交付包，只存于 macOS 钥匙串。
- 模型接口地址固定为 DeepSeek 与阿里云官方主机，不接受任意自定义地址。
- 明确第一版只读公开数据，不保存登录凭据，也不兑换重置卡。
